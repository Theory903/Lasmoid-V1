# LasmoidV1 — requirements.txt (2026)
# Core
torch>=2.6.0              # SDPA FlashAttn-2, NTK RoPE, torch.compile stable
torchvision>=0.21.0
safetensors>=0.4.5        # preferred checkpoint format
tiktoken>=0.9.0           # GPT-4o tokeniser support

# Utilities
numpy>=2.0.0
tqdm>=4.67.0
accelerate>=1.3.0         # device_map, multi-GPU / MPS helpers

# Optional — CUDA quantisation kernels (skipped on MPS/CPU)
# triton>=3.1.0           # Uncomment on CUDA systems for Triton fp8 kernels
